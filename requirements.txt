# Core orchestration
smolagents>=1.3
litellm>=1.48

# UI
streamlit>=1.38

# Config / validation
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# Persistence
sqlalchemy>=2.0

# Logging
loguru>=0.7

# --- Added in later phases, listed here so requirements.txt stays the
# single source of truth. Uncomment as each phase is implemented. ---

# Phase 5 - export pipeline (PPTX + Notebook prioritized; DOCX/PDF out of
# scope for now per trimmed plan)
python-pptx>=1.0
nbformat>=5.10
# python-docx>=1.1
# weasyprint>=62.0

# Phase 6 - vector store / memory (cut from trimmed scope, kept commented
# in case it's revisited)
# chromadb>=0.5

# Search tool backend (Phase 1 stub - not yet wired to Tavily)
# tavily-python>=0.5
