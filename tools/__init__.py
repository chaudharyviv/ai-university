"""
Tools are plain, typed functions agents can call. Each tool should:
  - do one thing
  - validate its own inputs
  - raise clear exceptions rather than failing silently
  - be safe to call repeatedly (idempotent where possible)
"""
